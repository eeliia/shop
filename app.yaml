runtime: python311

instance_class: F1

env_variables:
  DJANGO_SETTINGS_MODULE: "local_business_website.settings"

handlers:
- url: /static
  static_dir: staticfiles
  secure: always

- url: /media
  static_dir: media
  secure: always

- url: /.*
  script: auto
  secure: always